{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "scripts": {
    "tsc": "lerna run tsc",
    "publish": "lerna run tsc && lerna publish",
    "test": "lerna run test --stream",
    "coverage": "lerna run test:coverage"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/jest": "^29.5.11",
    "jest": "^29.7.0",
    "lerna": "^8.0.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.2"
  }
}
