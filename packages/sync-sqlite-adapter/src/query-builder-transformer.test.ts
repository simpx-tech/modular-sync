describe('QueryBuilderTransformer', () => {
  describe('transform', () => {
    it('should apply select', () => {

    });

    it('should apply where', () => {

    });

    it('should apply order by', () => {

    });

    it('should apply limit', () => {

    });

    it('should apply skip', () => {

    });
  });
});