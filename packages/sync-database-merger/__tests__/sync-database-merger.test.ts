describe("Sync Database Merger", () => {
  describe("runSetup", () => {
    it("should helpers the database tables and run repositories, auth and domain setups", async () => {
  })

  describe("receiveAll", () => {
    it.todo("should fail if domain is already migrated (should sync instead)")

    it.todo("should create the repository domain if it doesn't exist yet")

    it.todo("should allow receive all entities from a domain")

    it.todo("should allow receive the same entities without problem (and not create duplicates)")
  });

  describe("sendAll", () => {});

  describe("sync", () => {});
});